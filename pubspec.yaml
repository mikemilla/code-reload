name: branch_push
description: Branch-based code push for Flutter apps. Maps git branches to Shorebird tracks for instant OTA updates.
version: 0.1.0

environment:
  sdk: ^3.0.0

dependencies:
  args: ^2.4.2
  path: ^1.8.3
  yaml: ^3.1.2
  yaml_edit: ^2.2.1

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0

executables:
  branch_push:
